name: retip
description: "Retip is a music player built using Flutter"
publish_to: "none"

version: 1.0.0+26

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
