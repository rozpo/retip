import 'package:flutter/material.dart';

class CreatePlaylistAlert extends StatelessWidget {
  const CreatePlaylistAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
