import 'package:flutter/material.dart';
import '../../../../widgets/atoms/rp_icon_button.dart';

class SearchTracksButton extends StatelessWidget {
  const SearchTracksButton({super.key});

  @override
  Widget build(BuildContext context) {
    return RpIconButton(
      icon: Icons.search,
      onPressed: () {},
    );
  }
}
