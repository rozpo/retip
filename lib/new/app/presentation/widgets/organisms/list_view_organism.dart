import 'package:flutter/material.dart';

class ListViewOrganism extends StatelessWidget {
  const ListViewOrganism({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
