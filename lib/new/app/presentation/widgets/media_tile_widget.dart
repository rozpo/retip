import 'package:flutter/material.dart';

class MediaTileWidget extends StatelessWidget {
  const MediaTileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
