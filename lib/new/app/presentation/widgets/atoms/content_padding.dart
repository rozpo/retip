import 'package:flutter/material.dart';

class ContentPadding extends StatelessWidget {
  const ContentPadding({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.square(dimension: 8);
  }
}
